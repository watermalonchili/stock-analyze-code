# 股票形态相似性分析系统 — 系统使用说明书

> 本文面向**最终使用者**，讲解各功能在界面上的操作流程。部署/启动请参阅《使用说明书》。

## 一、界面总览

系统登录后（浏览器打开 `http://localhost:8080`）为「MA均线分析平台」，整体分三大区域：

```
┌────────────┬──────────────────────────────┬──────────────┐
│  左侧栏     │        主工作区               │  右侧栏       │
│            │  ┌─ 模型生成 ─ 模型使用 ─┐    │              │
│ · 股票列表  │  │  （标签页切换）        │    │ · 自定义模式 │
│ · 分析维度  │  │                       │    │   （卡片）   │
│ · MA 指标   │  │   主图 / 结果 / 控制台  │    │              │
│ · 时间范围  │  └───────────────────────┘    │              │
│ · 股票池    │                               │              │
└────────────┴──────────────────────────────┴──────────────┘
```

- **左侧栏**：选择股票、分析维度、均线指标、查找时间范围、股票池。
- **主工作区**：两个标签页 ——「模型生成」（检索 + 训练）、「模型使用」（实时扫描）。
- **右侧栏**：用户自定义模式的卡片。

---

## 二、两种分析维度

系统支持两种形态描述方式，在左侧栏「分析维度选择」切换：

| 维度 | 含义 | 适合场景 |
|---|---|---|
| **均线形态特征（MA）** | 基于多条均线的交叉/纠缠等结构特征描述形态 | 捕捉均线纠缠、金叉死叉等 |
| **K线趋势骨架（KLINE）** | 基于 K 线拐点连成的趋势骨架描述形态 | 捕捉整体走势轮廓 |

切换后，后续的框选、特征提取、检索、训练都会按该维度执行。

---

## 三、核心功能一：形态检索（历史查找）

### 第 1 步：选择股票与范围

1. 左侧「全部股票 / 我的自选」列表点击选中一只股票（主图显示其 K 线）。
2. 「分析维度选择」选「均线形态特征」或「K线趋势骨架」。
3. 若选均线模式，勾选「基础 MA 指标」（MA4/8/12/16/20/47 等）。
4. 「设置查找历史时间范围」选择检索的起止日期。
5. 「请选择待查找股票集」设定检索范围（默认为全部股票）。

### 第 2 步：框选形态片段

在主图上用鼠标**框选**一段历史 K 线作为「模板」（Brush 拖拽选择区间）。

### 第 3 步：提取特征 / 生成骨架

- **均线模式**：点击「提取均线特征」→ 弹出「均线形态特征提取」窗口。
- **K线模式**：点击「生成K线骨架」→ 弹出「交互式 K线趋势骨架生成」窗口。

**均线特征窗口**内可：
- 预览所选片段的走势；
- **点击图上的交叉点 / 纠缠点**，将其加入特征列表；
  - 红/绿圆点：均线上穿/下穿交叉点；
  - 黄色菱形：均线密集纠缠区间（相邻均线间距持续 <1%）。

**K线骨架窗口**内可：
- 左键点击图表任意位置，**添加/微调**骨架拐点；
- 查看「纯净趋势骨架」预览；
- 点击「确认骨架，下一步: 指标提取」。

### 第 4 步：补充语义特征（可选）

在特征窗口中：
1. 勾选系统自动提取的特征，并设置权重；
2. 在「自定义补充特征」输入文字描述，点击「AI 智能解析特征」→ 系统调大模型解析语义 → 弹出「AI 智能解析结果确认」，确认后加入特征；
3. 也可手动添加特征。

### 第 5 步：发起查找

点击「确认特征并进行历史查找」→ 系统在股票池内检索形态相似的标的/时段。

---

## 四、核心功能二：查看结果、打分与样本筛选

查找完成后进入「模式相似性查找结果详情」页。

### 1. 语义检索过滤漏斗

`总历史片段 → AI 语义逻辑过滤 → 数学形态相似`，直观展示各环节筛掉的片段数量。

### 2. 结果表格

每行显示匹配的股票名称/代码、起始/终止时间，以及**形态对比双图**（左侧基准 vs 右侧匹配）。

### 3. 人工打分评判（五星打分）

每条结果右侧有「人工打分评判」列，包含：

- **五星打分**（`★`，可打半星，范围 0~5 星）；
- **主观描述**输入框（可选）；
- **算法评分**：系统算出的相似度百分比，供打分参考。

打分的作用见下方「打分规则」。

### 4. 打分规则：高分/中性/低分

系统根据你的打分自动做三件事：

| 打分 | 含义 | 系统行为 |
|---|---|---|
| 4~5 星 | 高分（正样本）| 用于特征校准、AI 仿真的「好样本」种子 |
| 3 星 | 中性 | 仅记录，不触发特殊行为 |
| 1~2 星 | 低分（负样本）| **自动归入负样本库**（供训练时作反例）|

> **低星自动归负样本**：当你把某条结果打到 1~2 星时，系统自动将该片段提交到负样本库（`/submit_feedback`），无需手动操作。

### 5. 勾选用于训练

表格「是否用于训练模型」列的复选框，勾选你认为**形态匹配正确**的结果，作为正样本。

### 6. 自适应特征学件控制台

- 展示「可量化特征」（可动态校准数值）与「定性特征」（参考标签）；
- 打分后点击「**基于高分打分自动校准**」，系统按你打出的高分（≥4 星）样本，自动调整各特征的取值区间/权重。

### 7. 生成式数据增强（AI 仿真 + 代际迭代）

用 AI 生成更多仿真样本，扩充训练数据。面板顶部会标注当前「第 N 代」。

> AI 仿真为**条件生成**：始终以你在检索阶段提取的**形态骨架**（K线骨架 / 均线形态）为条件，保证生成的样本符合目标形态，而非随机生成。

**① 生成第 1 代**：
1. 在上方表格勾选 1~3 个真实匹配片段作为「种子」；
2. 点击「**AI 仿真生成**」→ 系统以骨架为条件，结合种子片段生成约 8 个仿真样本（第 1 代），以横向卡片形式展示。

**② 迭代下一代（一代、二代……）**：
1. 对当前代仿真样本打分（4~5 星 = 好、1~2 星 = 坏），并可输入反馈文字；
2. 点击「**基于反馈重新生成（第 N+1 代）**」→ 系统仍以骨架为条件，结合你的高分/低分反馈，递归生成下一代样本；
3. 当前代会被**归档**到「历史代际」折叠区，可展开查看、勾选复用。

**③ 勾选仿真样本**：每张仿真卡片带复选框，勾选后即纳入训练正样本。

---

## 五、核心功能三：训练个性化模型

用正/负样本微调出专属检测模型。

### 1. 正负样本来源

- **正样本**：你在结果表格「勾选」的片段，以及勾选的 AI 仿真样本；
- **负样本**：打分 1~2 星自动归入的「低分历史」片段，以及低分仿真样本。

### 2. 进入训练集管理舱

点击结果页顶部「基于勾选样本训练个性化模型」→ 弹出「训练集最终管理舱」：

- **正样本盒 / 负样本盒**：展示已收集的样本（含来源标签与星级），可删除不合适样本；
- 「+ AI 再合成几个」：用数据增强在正样本盒中再合成更多样本。

### 3. 发起训练

- 点击「**YOLO训练（含数据增强）**」→ 输入模型名称 → 训练 YOLO 检测器；
- 训练完成后，模型出现在右侧栏「自定义模式」卡片，可在「模型使用」中调用。

> 也支持小样本（原型网络/Adapter）训练路径，按正负样本微调出专属权重。

---

## 六、核心功能四：模型使用（实时匹配 / 回测 / 视觉检索）

切到主工作区「模型使用」标签页，可用已训练好的形态模型做三件事。

### 1. 实时匹配（多尺度融合扫描）

1. **选择目标模型**：从「自定义模型」下拉框选择已训练好的形态模型；
2. **模型预览**：查看所选模型的 K 线骨架 / 均线形态；
3. **扫描范围**：确认目标股票池与全局时间范围；
4. 点击「实时查找（最近走势）」→ 系统对股票池做**多阶段多尺度扫描**：
   - **视觉粗筛**（YOLO 视觉模型 + 原型网络打分）快速定位形态候选；
   - **数值相似度（DTW）**：对候选计算日线 / 周线的 DTW 分数，作为主要排序依据；
   - **规则精排**（LLM 语义规则）二次过滤候选；
   - 三个时间尺度并行：**日线 / 周线 / 5 分钟线**，分别给出匹配度；
5. 结果默认按 **DTW 日线相似度**从高到低排序，每行**三图并排**展示日 / 周 / 5 分钟三个尺度的形态对比与各自得分（视觉分 / DTW 分）。

### 2. 历史回测（形态胜率统计）

每条实时匹配结果旁有「**历史回测**」按钮，点击弹出「历史形态回测报告」抽屉：

- 选择预测周期（**5 / 10 / 20 日**）；
- 系统统计该形态在历史中的**出现次数**、**形态胜率**（出现后上涨概率）、**N 日平均涨幅**、**平均最大回撤**；
- 展示「未来路径束合图」（历史中每次出现后的走势叠加），以及逐条历史匹配详情卡片。

> 回测用于评估「该形态出现后未来走势如何」，不构成投资建议。

### 3. 视觉模型历史相似片段查找（YOLO 全景图）

用 YOLO 视觉模型在股票池内一次性定位所有与模型形态相似的历史片段：

- 「**YOLO 快速扫描**」：对目标股票池跑 YOLO 检测，输出所有命中候选；
- 「**YOLO 全景图**」：将某只股票 150 天 K 线渲染成一张大图，框出所有被 YOLO 命中的形态区间，可全屏查看、逐段点选。

> 视觉模型检索与数值检索可互补：视觉模型擅长快速粗筛、一次定位多段，数值检索擅长精确排序。

---

## 七、操作提示

- **框选区间**要尽量完整覆盖目标形态，检索效果更佳。
- **均线纠缠点**仅在「均线形态特征」模式下可见，需先勾选多条 MA 指标。
- **打分**是系统学习的核心信号：高分（4~5 星）引导模型靠近好样本，低分（1~2 星）自动积累负样本，请尽量为关键结果打分。
- **AI 仿真代际**：每一代生成后建议先打分再「重新生成」，迭代 2~3 代后样本质量通常更贴近你的偏好。
- **训练样本**越多、越典型，个性化模型效果越好；正样本建议至少几十条。
- **实时扫描**依赖已固化的模型，需先在「模型生成」中训练出模型。
- 所有检索/训练操作完成后，观察后端控制台日志可了解详细进度与异常。
