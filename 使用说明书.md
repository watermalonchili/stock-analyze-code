# 股票形态相似性分析系统 — 使用说明书

## 一、项目简介

本系统是一个面向 A 股的「K 线形态相似性计算 + 个性化模型生成与应用」交互式量化分析工具。核心能力：

- 用户在前端框选一段历史 K 线（或勾选形态特征）作为「模板」
- 系统在股票池中检索形态相似的标的 / 时段（K 线骨架 DTW、均线 Pearson）
- 支持用少量正负样本微调出专属检测模型（小样本学习 + YOLO 视觉检测）

技术栈：后端 Flask（Python）+ 前端 Vue 3 + ECharts + Element-Plus。

## 二、交付内容与目录结构

```
交付包/
├── 使用说明书.md          # 本文件
├── tool_sim/              # 前端（Vue3）
│   ├── src/               # 组件、视图
│   ├── public/data/       # 每只股票 日/周/月 线静态 JSON（前端直读）
│   └── package.json
├── tool_sim_back/         # 后端（Flask）★ 主开发目录
│   ├── app.py             # 入口，21 个 REST 路由
│   ├── config.py          # 配置（token / 路径）
│   ├── utils.py           # CSV 读取、MA 计算、相似度
│   ├── data/kline-data/   # ★ 行情数据（224 只股票 CSV，已内置）
│   ├── custom_modes/      # 微调模型权重（*.pt / *.pth / *.npy）
│   ├── models/weights/best.pt   # YOLO 基础权重
│   ├── few_shot_learning/ # 小样本 / YOLO 训练与推理
│   ├── csv_data/          # 少量样本 CSV
│   ├── requirements.txt
│   └── dockerfile / docker-compose.yml
├── test_data/             # 测试用股票池（494 只，复现 W 底测试用）
└── wbottom_test/          # W 底人工标注数据（复现测试用）
```

## 三、环境要求

| 组件 | 版本要求 |
|---|---|
| Python | 3.9 ~ 3.11（推荐 3.11）|
| Node.js | 16+（推荐 18）|
| 操作系统 | Windows / Linux / macOS |
| GPU（可选）| CUDA 11.8+（仅加速 YOLO 训练/推理，CPU 也能跑）|

## 四、后端部署（tool_sim_back）

### 1. 创建并激活虚拟环境

```bash
cd tool_sim_back

# Windows
python -m venv .venv
.venv\Scripts\activate

# Linux / macOS
python3 -m venv .venv
source .venv/bin/activate
```

### 2. 安装依赖

```bash
pip install -r requirements.txt
```

> ⚠️ `requirements.txt` 未包含以下依赖，需手动补装（YOLO / 画图 / LLM 用）：
>
> ```bash
> pip install ultralytics mplfinance openai
> ```

### 3. 数据说明

行情数据已内置在 `tool_sim_back/data/kline-data/`（224 只中证 500 成分股，日线 CSV，列：`timestamps,open,close,high,low,vol` 等）。

数据目录由环境变量 `DATA_FOLDER` 控制，默认指向项目内 `data/kline-data`。如需更换数据目录：

```bash
# Windows
set DATA_FOLDER=D:\your\data\kline-data
# Linux / macOS
export DATA_FOLDER=/your/data/kline-data
```

### 4. 启动

```bash
python app.py
```

启动后后端监听 `http://127.0.0.1:5000`。

## 五、前端部署（tool_sim）

```bash
cd tool_sim
npm install
npm run serve
```

启动后前端监听 `http://localhost:8080`，浏览器打开即可。前端已配置 CORS 与后端 `5000` 端口通信。

生产打包可用 `npm run build`（产物在 `dist/`）。

## 六、运行验证

1. 后端、前端分别启动。
2. 浏览器打开 `http://localhost:8080`。
3. 左侧「分析维度」选择 K 线趋势骨架 或 均线形态特征。
4. 主图上框选一段历史 → 提取特征 → 确认查找 → 后端在股票池检索相似形态。
5. 观察后端控制台日志，确认检索返回结果、前端双图展示正常。

## 七、复现 W 底测试（可选）

交付包内含 `test_data/`（494 只测试股票）与 `wbottom_test/`（人工标注的 W 底片段）。

```bash
cd tool_sim_back
python testCode/test_runner.py
```

测试结果输出到 `E:/gitClone/test_output/`（该路径硬编码在 `testCode/test_runner.py:12-14`，请按需修改 `TEST_DATA_DIR`、`OUTPUT_DIR`、`LABELS_DIR`）。

## 八、常见问题

**Q1：启动时报缺少 `ultralytics` / `mplfinance` / `openai`**
→ 见「四、2」手动补装。

**Q2：扫描类接口无数据 / 返回空**
→ 确认 `tool_sim_back/data/kline-data/` 下有 `{code}.csv`，或正确设置了 `DATA_FOLDER`。

**Q3：模型加载失败**
→ 确认 `custom_modes/` 与 `models/weights/best.pt` 未缺失；YOLO 基础权重（`yolov8n.pt` 等）首次训练时 ultralytics 会自动下载。

**Q4：GPU 不可用**
→ 不强制 GPU，CPU 可运行，仅训练/推理较慢。若需 GPU，安装对应 CUDA 版本的 torch：`pip install torch --index-url https://download.pytorch.org/whl/cu118`。

## 九、注意事项

- 本项目为学术/研究用途，不构成投资建议。
- 股票代码约定：6 开头 → 沪市（.SH），其余 → 深市（.SZ）。
- 部分历史遗留文件（`nouse/`、`models/test*.py` 等）未在交付包中，属废弃代码。
